{
  "name": "riotintu-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:ts": "cd src/RioTintU-VM/ts && npm run build",
    "dev": "npm run build:ts && vite",
    "build": "npm run build:ts && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
